CREATE TABLE temp (
  "productName" varchar,
  "bannerImageUrl" varchar,
  "images" varchar
);

CREATE TABLE products (
  "productName" varchar PRIMARY KEY,
  "bannerImageUrl" varchar,
  "images" varchar
);