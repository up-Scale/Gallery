CREATE TABLE temp (
  productName varchar,
  bannerImageUrl varchar,
  productImageUrls varchar
);

CREATE TABLE products (
  productName varchar PRIMARY KEY,
  bannerImageUrl varchar,
  productImageUrls varchar
);